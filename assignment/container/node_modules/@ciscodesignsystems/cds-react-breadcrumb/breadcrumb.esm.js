import { Children } from 'react';
import { jsx } from 'react/jsx-runtime';
import { CDSBreadcrumbSingle } from './components/breadcrumb-single.esm.js';
import { CDSBreadcrumbMultiple } from './components/breadcrumb-multiple.esm.js';
import { CDSBreadcrumbLink } from './components/breadcrumb-link.esm.js';

const CDSBreadcrumb = ({
  children = undefined,
  ...rest
}) => {
  const childrenCount = Children.count(children);
  if (childrenCount === 0) {
    return null;
  }
  return childrenCount === 1 ? /*#__PURE__*/jsx(CDSBreadcrumbSingle, {
    ...rest,
    children: children
  }) : /*#__PURE__*/jsx(CDSBreadcrumbMultiple, {
    ...rest,
    children: children
  });
};
CDSBreadcrumb.Link = CDSBreadcrumbLink;

export { CDSBreadcrumb, CDSBreadcrumb as default };
