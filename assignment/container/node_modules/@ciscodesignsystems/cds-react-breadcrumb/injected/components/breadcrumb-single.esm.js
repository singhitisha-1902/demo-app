import React from 'react';
import { cx } from '@ciscodesignsystems/cds-component-utilities';
import styles from '../breadcrumb.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSBreadcrumbSingle = ({
  children = undefined,
  className = '',
  ...rest
}) => /*#__PURE__*/jsx("nav", {
  className: cx(styles, 'cds-breadcrumb', className),
  ...rest,
  children: children && React.Children.map(children, child => {
    if ( /*#__PURE__*/React.isValidElement(child)) {
      return /*#__PURE__*/React.cloneElement(child, {
        withArrow: true
      });
    }
    return child;
  })
});

export { CDSBreadcrumbSingle };
