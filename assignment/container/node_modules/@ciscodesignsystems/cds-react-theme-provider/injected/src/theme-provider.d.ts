import type { ReactNode } from 'react';
import type { CDSBrandType, CDSThemeType } from './types';
export type CDSThemeProviderProps = {
    /**
     * This represents the theme that will be applied to the child components
     */
    theme?: CDSThemeType;
    /**
     * This represents the brand that will be applied to the child components
     */
    brand?: CDSBrandType;
    /**
     * This represents the child elements or components that will be wrapped or modified by the CDSThemeProvider
     */
    children?: ReactNode;
};
export declare const CDSThemeProvider: ({ brand, theme, children, }: CDSThemeProviderProps) => import("react/jsx-runtime").JSX.Element;
export default CDSThemeProvider;
