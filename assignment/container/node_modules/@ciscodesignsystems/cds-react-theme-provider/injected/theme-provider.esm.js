import { jsx } from 'react/jsx-runtime';

const CDSThemeProvider = ({
  brand = 'magnetic',
  theme = 'light',
  children = 'undefined'
}) => {
  const val = `${brand}-${theme}`;
  return /*#__PURE__*/jsx("div", {
    "data-cds-theme": val,
    children: children
  });
};

export { CDSThemeProvider, CDSThemeProvider as default };
