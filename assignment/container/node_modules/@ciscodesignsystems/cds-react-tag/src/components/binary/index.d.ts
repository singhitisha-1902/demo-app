import { type HTMLProps } from 'react';
import { type CDSDefaultTagProps } from '../default-tag';
export type CDSTagBinaryProps = {
    /**
     * Determines the state of the `CDSTag.Binary`.
     * Accepts a boolean value `true` for the `Active` tag and `false` for the  `Inactive`
     * @default true
     */
    active?: boolean;
} & Omit<CDSDefaultTagProps, 'onClose' | 'prefix' | 'children' | 'icon'> & Omit<HTMLProps<HTMLDivElement>, 'children' | 'size'>;
export declare const CDSTagBinary: import("react").ForwardRefExoticComponent<Omit<CDSTagBinaryProps, "ref"> & import("react").RefAttributes<HTMLDivElement>>;
