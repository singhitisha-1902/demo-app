import { forwardRef } from 'react';
import { cx } from '@ciscodesignsystems/cds-component-utilities';
import styles from '../../header.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSHeaderLogoLink = /*#__PURE__*/forwardRef((props, ref) => {
  const {
    as,
    children,
    className,
    ...rest
  } = props;
  const classNames = cx(styles, 'cds-header__logo__link', className);
  const Component = as || 'a';
  return /*#__PURE__*/jsx(Component, {
    role: "link",
    className: classNames,
    ref: ref,
    ...rest,
    children: children
  });
});
CDSHeaderLogoLink.displayName = 'CDSHeaderLogoLink';

export { CDSHeaderLogoLink, CDSHeaderLogoLink as default };
