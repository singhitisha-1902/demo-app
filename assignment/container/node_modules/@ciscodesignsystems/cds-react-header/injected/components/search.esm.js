import { MagnifyingGlass } from 'phosphor-react';
import { cx } from '@ciscodesignsystems/cds-component-utilities';
import { CDSTextInput } from '@ciscodesignsystems/cds-react-text-input/injected';
import styles from '../header.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSHeaderSearch = ({
  placeholder = 'Search',
  ...rest
}) => {
  return /*#__PURE__*/jsx(CDSTextInput, {
    placeholder: placeholder,
    className: cx(styles, 'cds-header__search'),
    size: "md",
    fixedWidth: true,
    "data-testid": "headerSearchInput",
    prefix: /*#__PURE__*/jsx(MagnifyingGlass, {
      size: "24px",
      weight: "bold",
      "data-testid": "headerSearchIcon"
    }),
    ...rest
  });
};

export { CDSHeaderSearch, CDSHeaderSearch as default };
