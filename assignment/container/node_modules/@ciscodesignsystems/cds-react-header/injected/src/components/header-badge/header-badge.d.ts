/**
 * @typedef CDSHeaderBadge
 *
 * @param {Object} props Component props.
 * @param {string} size Badge sizes
 */
import type { ReactNode } from 'react';
import type { TSentiment, TSentimentProminence } from '@ciscodesignsystems/cds-component-types';
import { type CDSHeaderBadgeIconLocations, CDSHeaderBadgeSizes } from './types';
export interface CDSHeaderBadgeProps {
    className?: string;
    children?: ReactNode;
    /**
     * Content to be displayed in the badge.
     */
    content?: string | number;
    /**
     * The size of the badge to render
     */
    size?: keyof typeof CDSHeaderBadgeSizes;
    /**
     * The sentiment of the badge to render
     */
    sentiment: TSentiment;
    /**
     * The prominence of the badge to render
     */
    prominence?: TSentimentProminence;
    /**
     * Aligns the icon within the badge with inner
     * and either left or right
     */
    childAlign?: CDSHeaderBadgeIconLocations;
}
export declare const CDSHeaderBadge: ({ className, children, content, size, sentiment, prominence, childAlign, }: CDSHeaderBadgeProps) => import("react/jsx-runtime").JSX.Element;
export default CDSHeaderBadge;
