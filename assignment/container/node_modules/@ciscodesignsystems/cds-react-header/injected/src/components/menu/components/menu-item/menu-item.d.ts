import React, { type ComponentPropsWithRef, type ReactElement, type ReactNode } from 'react';
import type { IconProps } from 'phosphor-react';
import type { CDSButtonProps } from '@ciscodesignsystems/cds-react-button';
import type { CDSTagProps } from '@ciscodesignsystems/cds-react-tag';
import type { CDSToggleProps } from '@ciscodesignsystems/cds-react-toggle';
export interface CDSMenuItemProps extends Omit<ComponentPropsWithRef<'li'>, 'title'> {
    /**
     * The Leading Icon for the menu item.
     *
     * @default false
     */
    icon?: ReactElement<IconProps> | false;
    /**
     * A required main line title for the text section of the menu item.
     *
     * @required
     */
    title: ReactNode;
    /**
     * The action to be displayed on the right of the menu item.
     *
     * @required
     */
    action: ReactElement<CDSButtonProps<'button'>> | ReactElement<CDSTagProps> | ReactElement<CDSToggleProps>;
    /**
     * An optional label for the text section of the menu item.
     *
     * @default ''
     */
    label?: ReactNode;
    /**
     * An optional subtitle for the text section of the menu item.
     *
     * @default ''
     */
    subtitle?: ReactNode;
    /**
     * Controls when to truncate the content text
     *
     * @default false
     */
    truncate?: boolean;
}
export declare const getTitle: (node: ReactNode) => string;
export declare const CDSMenuItem: React.ForwardRefExoticComponent<Omit<CDSMenuItemProps, "ref"> & React.RefAttributes<HTMLLIElement>>;
export default CDSMenuItem;
