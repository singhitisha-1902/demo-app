import { type ComponentPropsWithRef } from 'react';
import type { CDSCheckboxSize } from './types';
export interface CDSCheckboxProps extends Omit<ComponentPropsWithRef<'input'>, 'size'> {
    /**
     * Size of the checkbox.
     *
     * Accepts `sm` and `md`.
     *
     * @default md
     */
    size?: CDSCheckboxSize;
    /**
     * Determines if the `CDSCheckbox` should be rendered in its indeterminate state.
     *
     * Accepts `true` or `false`.
     *
     * @default false
     */
    indeterminate?: boolean;
}
export declare const CDSCheckbox: import("react").ForwardRefExoticComponent<Omit<CDSCheckboxProps, "ref"> & import("react").RefAttributes<HTMLInputElement>>;
export default CDSCheckbox;
