'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var react = require('react');
var context = require('../context.cjs.js');

const usePlacement = isNested => {
  const {
    placement
  } = react.useContext(context.CDSDropdownContext);
  if (isNested) {
    return placement === 'left' ? 'right-start' : 'left-start';
  }
  return placement === 'left' ? 'bottom-start' : 'bottom-end';
};

exports.usePlacement = usePlacement;
