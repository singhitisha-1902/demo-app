import { useContext, createContext } from 'react';

const DropdownPanelContext = /*#__PURE__*/createContext({
  allowHover: false,
  isOpen: false,
  tree: null,
  setActiveIndex: () => {},
  activeIndex: null,
  getItemProps: () => ({})
});
const useDropdownPanelContext = () => useContext(DropdownPanelContext);

export { DropdownPanelContext, useDropdownPanelContext };
