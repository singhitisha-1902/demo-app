import { type ReactElement } from 'react';
import type { CDSDropdownPanelProps } from '../../types';
export declare const CDSDropdownPanel: {
    ({ activeIndex, allowHover, children, context, getFloatingProps, getItemProps, isNested, isOpen, listItemsRef, refs, setActiveIndex, strategy, tree, x, y, className, root, }: CDSDropdownPanelProps): ReactElement;
    displayName: string;
};
export default CDSDropdownPanel;
