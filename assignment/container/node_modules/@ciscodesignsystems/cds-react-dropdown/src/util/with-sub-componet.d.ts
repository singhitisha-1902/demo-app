import { type ElementType } from 'react';
export declare const withSubComponent: <Component extends ElementType, SubComponents>(component: Component, subComponents: SubComponents) => Component & SubComponents;
