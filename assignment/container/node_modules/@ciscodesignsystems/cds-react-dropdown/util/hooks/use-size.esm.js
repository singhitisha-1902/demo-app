import { useContext } from 'react';
import { CDSDropdownContext } from '../context.esm.js';

const useSize = () => {
  const {
    size
  } = useContext(CDSDropdownContext);
  return size || 'md';
};

export { useSize };
