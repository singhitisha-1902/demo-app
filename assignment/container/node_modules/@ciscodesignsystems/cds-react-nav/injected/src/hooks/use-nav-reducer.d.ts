/// <reference types="react" />
import type { CDSNavReducerAction, CDSNavReducerState } from '../types';
export declare const useNavReducer: () => [CDSNavReducerState, import("react").Dispatch<CDSNavReducerAction>];
