import { type ElementType, type FC, type ReactElement } from 'react';
import type { CDSNavItemProps } from '../../types';
type CDSNavSwitcherProps<C extends ElementType = 'a'> = CDSNavItemProps<C> & {
    subHeading: string;
};
export type CDSNavSwitcherComponent = FC & (<C extends ElementType = 'a'>(props: CDSNavSwitcherProps<C>) => ReactElement | null);
export declare const CDSNavSwitcher: CDSNavSwitcherComponent;
export default CDSNavSwitcher;
