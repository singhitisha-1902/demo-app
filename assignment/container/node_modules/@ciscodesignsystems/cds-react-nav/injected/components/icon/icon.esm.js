import { memo } from 'react';
import { CDSNavIconMap } from '@ciscodesignsystems/cds-react-icons/injected';
import { jsx } from 'react/jsx-runtime';

const CDSNavIcon = /*#__PURE__*/memo(({
  icon,
  ...rest
}) => {
  if (typeof icon === 'string') {
    const Icon = CDSNavIconMap[icon];
    if (!Icon) {
      throw new Error(`CDSNavIcon: Invalid icon kind "${icon}"`);
    }
    return /*#__PURE__*/jsx(Icon, {
      ...rest
    });
  }
  return icon;
});
CDSNavIcon.displayName = 'CDSNav.Icon';

export { CDSNavIcon };
