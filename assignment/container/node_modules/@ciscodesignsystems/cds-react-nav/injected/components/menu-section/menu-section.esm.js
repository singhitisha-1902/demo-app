import { forwardRef } from 'react';
import { cx } from '@ciscodesignsystems/cds-component-utilities';
import { useNavContext } from '../../context/nav-context.esm.js';
import styles from './menu-section.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSNavMenuSection = /*#__PURE__*/forwardRef((props, ref) => {
  const {
    children,
    className,
    ...rest
  } = props;
  const {
    menuGroupGap
  } = useNavContext();
  const classes = cx(styles, 'cds-nav__menu__section', {
    [`cds-nav__menu__section--margin-top-${menuGroupGap}`]: menuGroupGap
  }, className);
  return /*#__PURE__*/jsx("div", {
    ref: ref,
    className: classes,
    ...rest,
    children: children
  });
});
CDSNavMenuSection.displayName = 'CDSNav.Section';

export { CDSNavMenuSection, CDSNavMenuSection as default };
