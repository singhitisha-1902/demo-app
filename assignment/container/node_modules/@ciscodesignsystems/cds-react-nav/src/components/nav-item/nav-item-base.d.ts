import { type ElementType, type FC, type ReactElement } from 'react';
import { type CDSNavItemProps } from '../../types';
type CDSNavItemBaseProps<C extends ElementType = 'a'> = CDSNavItemProps<C> & {
    canShowCaret?: boolean;
};
export type CDSNavBaseItemComponent = FC & (<C extends ElementType = 'a'>(props: CDSNavItemBaseProps<C>) => ReactElement | null);
export declare const CDSNavItemBase: CDSNavBaseItemComponent;
export default CDSNavItemBase;
