import { type ElementType, type FC, type ReactElement, type ReactNode } from 'react';
import type { PolymorphicComponentPropWithRef } from '@ciscodesignsystems/cds-component-types';
export type CDSMenuLinkBaseProps = {
    /**
     * Element to display next to the link
     */
    children: string;
    /**
     * Icon to display left of the link
     */
    icon?: ReactElement;
    /**
     * Element to display right of the text
     */
    suffix?: ReactNode;
};
export type CDSMenuLinkProps<C extends ElementType = 'a'> = PolymorphicComponentPropWithRef<C, CDSMenuLinkBaseProps & ({
    menu?: ReactElement;
} | {
    isSelected?: boolean;
})>;
type CDSMenuLinkComponent = FC & (<C extends ElementType = 'a'>(props: CDSMenuLinkProps<C>) => ReactElement | null);
export declare const CDSMenuLink: CDSMenuLinkComponent;
export default CDSMenuLink;
