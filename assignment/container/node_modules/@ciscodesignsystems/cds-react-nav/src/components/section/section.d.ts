import { type ReactElement, type ReactNode } from 'react';
export interface CDSNavSectionProps {
    children: ReactNode;
}
export declare const CDSNavSection: {
    ({ children }: CDSNavSectionProps): (string | number | ReactElement<any, string | import("react").JSXElementConstructor<any>> | Iterable<ReactNode>)[];
    displayName: string;
};
