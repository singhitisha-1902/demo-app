'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var jsxRuntime = require('react/jsx-runtime');
var react = require('react');
var cdsReactIcons = require('@ciscodesignsystems/cds-react-icons');

const CDSNavIcon = /*#__PURE__*/react.memo(({
  icon,
  ...rest
}) => {
  if (typeof icon === 'string') {
    const Icon = cdsReactIcons.CDSNavIconMap[icon];
    if (!Icon) {
      throw new Error(`CDSNavIcon: Invalid icon kind "${icon}"`);
    }
    return jsxRuntime.jsx(Icon, {
      ...rest
    });
  }
  return icon;
});
CDSNavIcon.displayName = 'CDSNav.Icon';

exports.CDSNavIcon = CDSNavIcon;
