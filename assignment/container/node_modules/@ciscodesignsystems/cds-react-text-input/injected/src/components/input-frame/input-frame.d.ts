import { type CDSFrameInputProps } from '../../types';
export declare const CDSInputFrame: ({ id, children, prefix, suffix, size, sentiment, bordered, disabled, label, optional, tooltip, className, fixedWidth, type, readOnly, required, ...rest }: CDSFrameInputProps) => import("react/jsx-runtime").JSX.Element;
