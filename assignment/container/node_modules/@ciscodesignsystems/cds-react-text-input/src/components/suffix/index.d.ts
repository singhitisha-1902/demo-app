import type { Dispatch, SetStateAction } from 'react';
import type { CDSTextInputProps } from '../../text-input';
import type { CDSTextInputPasswordProps } from '../../types';
export type SuffixCompType = Pick<CDSTextInputProps, 'type' | 'suffix' | 'onClear' | 'disabled'> & CDSTextInputPasswordProps & {
    /**
     * type of input component.
     */
    type: string;
    /**
     * Determines whether to hide/show password.
     */
    showPassword?: boolean;
    /**
     * SetState function for hiding/showing password.
     */
    setShowPassword?: Dispatch<SetStateAction<boolean>>;
    /**
     * Checks if there is some value in the input field.
     */
    hasValue: boolean;
};
declare const SuffixComp: ({ type, suffix, showPassword, setShowPassword, onClear, disabled, hasValue, showPasswordText, hidePasswordText, }: SuffixCompType) => import("react/jsx-runtime").JSX.Element;
export default SuffixComp;
