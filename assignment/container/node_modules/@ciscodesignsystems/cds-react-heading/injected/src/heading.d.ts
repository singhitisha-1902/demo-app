import { type ElementType, type FC, type ReactElement } from 'react';
import { type Margin, type PolymorphicComponentPropWithRef } from '@ciscodesignsystems/cds-component-types';
import { type CDSHeadingSize } from './types';
export type CDSHeadingProps<C extends ElementType> = PolymorphicComponentPropWithRef<C, {
    /**
     * Determines the size of the `Heading`.
     * @default 'section'
     */
    size?: CDSHeadingSize;
    /**
     * Provides access to add margin to the heading.
     * Accepts type MarginNumber
      | [MarginNumber]
      | [MarginNumber, MarginNumber]
      | [MarginNumber, MarginNumber, MarginNumber]
      | [MarginNumber, MarginNumber, MarginNumber, MarginNumber]
      | MarginObject
     * @default 0
     */
    margin?: Margin;
}>;
type CDSHeadingComponent = FC & (<C extends ElementType = 'h1'>(props: CDSHeadingProps<C>) => ReactElement | null);
export declare const CDSHeading: CDSHeadingComponent;
export default CDSHeading;
