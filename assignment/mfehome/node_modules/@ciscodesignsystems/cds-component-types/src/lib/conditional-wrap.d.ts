import { type ReactElement } from 'react';
export type ConditionalWrapProps<T> = {
    /**
     * children prop represents the direct children of ConditionalWrap component.
     *
     */
    children: T;
    /**
     * takes a boolean value that indicates whether the wrap should be rendered or not.
     *
     */
    condition?: boolean;
    /**
     * returns the wrapped component if the condition prop returns true.
     *
     */
    wrap: (children: T) => ReactElement;
};
export declare const ConditionalWrap: <T>({ children, condition, wrap, }: ConditionalWrapProps<T>) => ReactElement;
