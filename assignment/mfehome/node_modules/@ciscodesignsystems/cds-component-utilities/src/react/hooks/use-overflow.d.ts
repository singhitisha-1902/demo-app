import { Children, type RefObject } from 'react';
export declare const useOverflow: <T extends HTMLElement>(children: Parameters<typeof Children.toArray>[0], childQuerySelector: string, offsetPadding: number) => {
    childArray: ReturnType<typeof Children.toArray>;
    ref: RefObject<T>;
    visibleItems: number;
};
