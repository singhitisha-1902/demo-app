import { cx } from '@ciscodesignsystems/cds-component-utilities';
import { CDSButton } from '@ciscodesignsystems/cds-react-button/injected';
import styles from './suffix.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const PasswordSuffix = ({
  showPassword,
  togglePassword,
  hidePasswordText = 'Hide',
  showPasswordText = 'Show'
}) => {
  const handleOnClick = event => {
    event.stopPropagation();
    togglePassword();
  };
  const handleOnBlur = event => {
    event.stopPropagation();
  };
  return /*#__PURE__*/jsx(CDSButton, {
    variant: "tertiary",
    onClick: handleOnClick,
    onBlur: handleOnBlur,
    className: cx(styles, 'cds-text-input__toggle-password'),
    children: showPassword ? hidePasswordText : showPasswordText
  });
};
var PasswordSuffix$1 = PasswordSuffix;

export { PasswordSuffix$1 as default };
