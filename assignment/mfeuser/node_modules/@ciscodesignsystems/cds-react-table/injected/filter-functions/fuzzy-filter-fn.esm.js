import { rankItem } from '@tanstack/match-sorter-utils';

const fuzzyFilterFn = (row, columnId, value, addMeta) => {
  const itemRank = rankItem(row.getValue(columnId), value);
  addMeta({
    itemRank
  });
  return itemRank.passed;
};

export { fuzzyFilterFn };
