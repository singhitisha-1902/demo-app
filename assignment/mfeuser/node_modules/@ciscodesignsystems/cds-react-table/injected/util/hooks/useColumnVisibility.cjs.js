'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var react = require('react');

const useColumnVisibility = ({
  columns
}) => {
  const columnVisibilityState = react.useMemo(() => {
    const columnVisibilityInitialStateObj = columns.reduce((prev, curr) => {
      var _a;
      return {
        ...prev,
        [`${curr.id}`]: !((_a = curr.meta) === null || _a === void 0 ? void 0 : _a.isHidden)
      };
    }, {});
    return columnVisibilityInitialStateObj;
  }, [columns]);
  const [columnVisibility, setColumnVisibility] = react.useState(columnVisibilityState);
  react.useEffect(() => {
    setColumnVisibility(columnVisibilityState);
  }, [columnVisibilityState]);
  return {
    columnVisibility,
    setColumnVisibility
  };
};

exports.useColumnVisibility = useColumnVisibility;
