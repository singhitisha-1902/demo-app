import { type FilterMeta, type Row } from '@tanstack/react-table';
export declare const fuzzyFilterFn: <TData>(row: Row<TData>, columnId: string, value: string, addMeta: (meta: FilterMeta) => void) => boolean;
