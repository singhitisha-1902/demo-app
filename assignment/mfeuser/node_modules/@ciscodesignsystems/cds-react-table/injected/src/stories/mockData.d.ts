import { type ColumnDef } from '@tanstack/react-table';
export type Person = {
    id?: number;
    adminOnly?: boolean;
    email: string;
    firstName: string;
    ip: string;
    lastName: string;
    live: boolean;
    usage?: number;
};
export declare const columns: ColumnDef<Person>[];
export declare const additionalPinnedColumns: ColumnDef<Person>[];
export declare const people: Person[];
