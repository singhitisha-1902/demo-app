import { type ReactNode } from 'react';
import { type CDSFlexProps } from '@ciscodesignsystems/cds-react-flex';
import { type CDSTableOnGlobalSearchChangeFn } from '../../types';
export interface CDSTableFilterBar extends Omit<CDSFlexProps, 'children'> {
    /**
     * Children to display in the filter drawer
     */
    filterDrawerChildren?: ReactNode;
    /**
     * ReactNode to display in the button that toggles the filter drawer
     */
    filterDrawerToggleText?: ReactNode;
    /**
     * Toggles the global search textbox on the left of the filter bar
     */
    showGlobalSearch?: boolean;
    /**
     * Placeholder to show in the global search input
     */
    globalSearchPlaceholder?: string;
    /**
     * ReactNode to display between the global search input and the filter drawer toggle
     */
    keyFilterChildren?: ReactNode;
    /**
     * ReactNode to display optional actions at the end of FilterBar.
     */
    children?: ReactNode;
    /**
     * A callback to be executed when search changes when using table with manualPagination.
     */
    onGlobalSearchChange?: CDSTableOnGlobalSearchChangeFn;
}
export declare const CDSTableFilterBar: import("react").ForwardRefExoticComponent<Omit<CDSTableFilterBar, "ref"> & import("react").RefAttributes<HTMLDivElement>>;
