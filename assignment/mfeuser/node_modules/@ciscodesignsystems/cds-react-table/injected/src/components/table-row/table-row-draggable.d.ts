import type { ComponentPropsWithoutRef } from 'react';
import type { Row, RowData, Table } from '@tanstack/react-table';
import type { CDSTableOnReorderRowFn } from '../../types';
export interface TableRowDraggableProps<TData extends RowData> extends ComponentPropsWithoutRef<'tr'> {
    table: Table<TData>;
    row: Row<TData>;
    onReorderRow: CDSTableOnReorderRowFn<TData>;
    pinnedPositions?: {
        left: number[];
        right: number[];
    };
}
export declare const TableRowDraggable: <TData extends unknown>({ table, row, onReorderRow, pinnedPositions, ...rest }: TableRowDraggableProps<TData>) => import("react/jsx-runtime").JSX.Element;
