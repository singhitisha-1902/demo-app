import type { ComponentPropsWithoutRef } from 'react';
import { type Cell as CellType, type RowData } from '@tanstack/react-table';
interface CDSTableCellProps<TData extends RowData, TValue> extends ComponentPropsWithoutRef<'td'> {
    cell: CellType<TData, TValue>;
    pinnedPosition?: number;
}
export declare const TableCell: <TData extends unknown, TValue>({ cell, pinnedPosition, children, }: CDSTableCellProps<TData, TValue>) => import("react/jsx-runtime").JSX.Element;
export {};
