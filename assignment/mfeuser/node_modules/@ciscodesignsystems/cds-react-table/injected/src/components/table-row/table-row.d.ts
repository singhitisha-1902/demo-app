import { type ComponentPropsWithRef, type ReactElement, type ReactNode } from 'react';
import type { Row, RowData } from '@tanstack/react-table';
export interface TableRowProps<TData extends RowData> extends ComponentPropsWithRef<'tr'> {
    row: Row<TData>;
    pinnedPositions?: {
        left: number[];
        right: number[];
    };
    dragComponent?: ReactNode;
}
export declare const TableRow: <TData extends unknown>(props: TableRowProps<TData>, ref: HTMLTableRowElement) => ReactElement;
