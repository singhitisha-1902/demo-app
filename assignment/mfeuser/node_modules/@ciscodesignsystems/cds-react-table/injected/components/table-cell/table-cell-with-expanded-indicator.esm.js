import { CaretRight } from 'phosphor-react';
import { cx } from '@ciscodesignsystems/cds-component-utilities';
import { CDSButton } from '@ciscodesignsystems/cds-react-button/injected';
import styles from './table-cell-with-expanded-indicator.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const TableCellWithExpandedIndication = ({
  row
}) => {
  const className = cx(styles, 'cds-table__row--expandable__trigger', {
    'cds-table__row--expandable__trigger--expanded': row.getIsExpanded()
  });
  return row.getCanExpand() && /*#__PURE__*/jsx(CDSButton, {
    className: className,
    variant: "tertiary",
    onClick: () => row.toggleExpanded(),
    children: /*#__PURE__*/jsx(CaretRight, {
      size: 18,
      weight: "bold"
    })
  });
};

export { TableCellWithExpandedIndication };
