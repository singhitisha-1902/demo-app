import { cx } from '@ciscodesignsystems/cds-component-utilities';
import { CDSEmptyState } from '@ciscodesignsystems/cds-react-empty-state/injected';
import styles from './empty-state-info.module.esm.js';
import { jsxs, Fragment, jsx } from 'react/jsx-runtime';

const EmptyStateInfo = ({
  height,
  width,
  message,
  ...rest
}) => {
  const messageClasses = cx(styles, 'cds-table__empty-state__message');
  return /*#__PURE__*/jsxs(Fragment, {
    children: [/*#__PURE__*/jsx(CDSEmptyState.Illustration, {
      variant: "info",
      height: height,
      width: width,
      ...rest
    }), /*#__PURE__*/jsx(CDSEmptyState.Message, {
      className: messageClasses,
      children: message
    })]
  });
};

export { EmptyStateInfo };
