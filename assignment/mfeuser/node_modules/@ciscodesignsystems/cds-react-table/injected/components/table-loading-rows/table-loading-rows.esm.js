import { CDSSkeleton } from '@ciscodesignsystems/cds-react-skeleton/injected';
import { jsx, Fragment } from 'react/jsx-runtime';

const CDSTableLoadingRows = ({
  rows,
  columns
}) => {
  return /*#__PURE__*/jsx(Fragment, {
    children: [...new Array(rows).keys()].map(rowIndex => /*#__PURE__*/jsx("tr", {
      className: "cds-table__row",
      children: [...new Array(columns).keys()].map(cellIndex => /*#__PURE__*/jsx("td", {
        className: "cds-table__cell",
        children: /*#__PURE__*/jsx(CDSSkeleton, {
          height: "sm"
        })
      }, cellIndex))
    }, rowIndex))
  });
};

export { CDSTableLoadingRows };
