import { createContext, useContext } from 'react';

/**
 * Type any has to be used here in order for TableContext to be shared
 * Otherwise useContext would lose reference to TableContext in order
 * for consuming components to have access to it.
 */
const TableContext = /*#__PURE__*/createContext({
  globalFilter: '',
  setGlobalFilter: undefined,
  table: undefined,
  manualPagination: false
});
const useTableContext = () => useContext(TableContext);

export { TableContext, useTableContext };
