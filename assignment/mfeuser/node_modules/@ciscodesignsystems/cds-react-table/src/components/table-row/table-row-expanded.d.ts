import { type ComponentPropsWithRef, type ReactElement, type ReactNode } from 'react';
import type { Row, RowData } from '@tanstack/react-table';
export interface TableRowExpandedProps<TData extends RowData> extends ComponentPropsWithRef<'tr'> {
    row: Row<TData>;
    children?: ReactNode;
}
export declare const TableRowExpanded: <TData extends unknown>(props: TableRowExpandedProps<TData>, ref: HTMLTableRowElement) => ReactElement;
