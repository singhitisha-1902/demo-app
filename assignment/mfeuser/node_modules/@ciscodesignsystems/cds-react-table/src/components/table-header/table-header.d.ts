import { type ComponentPropsWithRef, type ReactElement } from 'react';
import { type Header as HeaderType, type RowData } from '@tanstack/react-table';
export interface TableHeaderProps<TData extends RowData, TValue> extends ComponentPropsWithRef<'th'> {
    header: HeaderType<TData, TValue>;
    loading?: boolean;
    pinnedPosition?: number;
}
export declare const TableHeader: <TData extends unknown, TValue>(props: TableHeaderProps<TData, TValue>, ref: HTMLTableCellElement) => ReactElement;
