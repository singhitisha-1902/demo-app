import { type Dispatch, type SetStateAction } from 'react';
import type { ColumnDef, ColumnOrderState, ColumnPinningState, RowData } from '@tanstack/react-table';
type UseColumnOrderProps<TData extends RowData> = {
    columnPinning: ColumnPinningState;
    columns: ColumnDef<TData>[];
};
type UseColumnOrderReturn = {
    columnOrder: ColumnOrderState;
    setColumnOrder: Dispatch<SetStateAction<ColumnOrderState>>;
};
export declare const useColumnOrder: <TData extends unknown>({ columnPinning, columns, }: UseColumnOrderProps<TData>) => UseColumnOrderReturn;
export {};
