import { type Dispatch, type SetStateAction } from 'react';
import type { ColumnDef, RowData, VisibilityState } from '@tanstack/react-table';
type UseColumnVisibilityProps<TData extends RowData> = {
    columns: ColumnDef<TData>[];
};
type UseColumnVisibilityReturn = {
    columnVisibility: VisibilityState;
    setColumnVisibility: Dispatch<SetStateAction<VisibilityState>>;
};
export declare const useColumnVisibility: <TData extends unknown>({ columns, }: UseColumnVisibilityProps<TData>) => UseColumnVisibilityReturn;
export {};
