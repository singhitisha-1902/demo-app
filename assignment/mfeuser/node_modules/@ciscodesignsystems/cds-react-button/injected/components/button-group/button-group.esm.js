import { forwardRef } from 'react';
import { cx } from '@ciscodesignsystems/cds-component-utilities';
import styles from './button-group.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSButtonGroup = /*#__PURE__*/forwardRef((props, ref) => {
  const {
    as,
    children,
    className,
    ...rest
  } = props;
  const Component = as ?? 'div';
  const classNames = cx(styles, 'cds-button__group', className);
  return /*#__PURE__*/jsx(Component, {
    ...rest,
    ref: ref,
    className: classNames,
    children: children
  });
});
CDSButtonGroup.displayName = 'CDSButton.Group';

export { CDSButtonGroup };
