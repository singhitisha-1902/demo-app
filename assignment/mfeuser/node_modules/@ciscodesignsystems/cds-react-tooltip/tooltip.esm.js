import { forwardRef } from 'react';
import { cx } from '@ciscodesignsystems/cds-component-utilities';
import { CDSPopover } from '@ciscodesignsystems/cds-react-popover';
import styles from './tooltip.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSTooltip = /*#__PURE__*/forwardRef((props, ref) => {
  const {
    className,
    ...rest
  } = props;
  const classNames = cx(styles, 'cds-tooltip', className);
  return /*#__PURE__*/jsx(CDSPopover, {
    className: classNames,
    role: "tooltip",
    ref: ref,
    ...rest
  });
});
CDSTooltip.displayName = 'CDSTooltip';
CDSTooltip.defaultProps = {
  placement: 'top',
  className: '',
  autoPlacement: false,
  disabled: false,
  showArrow: true,
  variant: 'default'
};

export { CDSTooltip, CDSTooltip as default };
