import { cx } from '@ciscodesignsystems/cds-component-utilities';
import styles from './title.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSEmptyStateTitle = ({
  className,
  children,
  ...rest
}) => {
  const classNames = cx(styles, 'cds-empty-state__title', className);
  return /*#__PURE__*/jsx("div", {
    className: classNames,
    ...rest,
    children: children
  });
};
CDSEmptyStateTitle.displayName = 'CDSEmptyState.Title';

export { CDSEmptyStateTitle };
