import { IcEmptyStateInfo, IcEmptyStatePositive, IcEmptyStateNegative } from '@ciscodesignsystems/cds-react-icons/injected';
import { jsx } from 'react/jsx-runtime';

const handleIllustrateState = status => {
  switch (status) {
    case 'negative':
      return IcEmptyStateNegative;
    case 'positive':
      return IcEmptyStatePositive;
    case 'info':
    default:
      return IcEmptyStateInfo;
  }
};
const CDSEmptyStateIllustration = ({
  className = '',
  variant = 'info',
  ...rest
}) => {
  const Svg = handleIllustrateState(variant);
  return /*#__PURE__*/jsx(Svg, {
    className: className,
    ...rest
  });
};
CDSEmptyStateIllustration.displayName = 'CDSEmptyState.Illustration';

export { CDSEmptyStateIllustration, handleIllustrateState };
