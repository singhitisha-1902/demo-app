import { cx } from '@ciscodesignsystems/cds-component-utilities';
import styles from './message.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSEmptyStateMessage = ({
  className,
  children,
  ...rest
}) => {
  const classNames = cx(styles, 'cds-empty-state__message', className);
  return /*#__PURE__*/jsx("p", {
    className: classNames,
    ...rest,
    children: children
  });
};
CDSEmptyStateMessage.displayName = 'CDSEmptyState.Message';

export { CDSEmptyStateMessage };
