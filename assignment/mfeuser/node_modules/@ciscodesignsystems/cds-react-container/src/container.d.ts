import { type ComponentPropsWithRef } from 'react';
import { type Margin } from '@ciscodesignsystems/cds-component-types';
import type { CDSContainerBackgroundObj } from './types';
export interface CDSContainerProps extends ComponentPropsWithRef<'div'> {
    /**
     * Handles adding a background image to the container. Can handle any format of background image that css can handle.
     *
     * Can accept a string:
     * ```
     * <CDSContainer background={`url(./some-image.png)`} />
     * ```
     *
     * Can also accept an object that modifes the position, repeat and size css properties of the background:
     * ```
     * <CDSContainer background={{ image: `url(./some-image.png)`, position: "top" }} />
     * ```
     *
     * @default `undefined`
     */
    background?: string | CDSContainerBackgroundObj;
    /**
     * Provides access to add margin to the container.
     *
     * Accepts type `MarginNumber`
     | `[MarginNumber]`
     | `[MarginNumber, MarginNumber]`
     | `[MarginNumber, MarginNumber, MarginNumber]`
     | `[MarginNumber, MarginNumber, MarginNumber, MarginNumber]`
     | `MarginObject`
     *
     * @default 0
     */
    margin?: Margin;
}
export declare const CDSContainer: import("react").ForwardRefExoticComponent<Omit<CDSContainerProps, "ref"> & import("react").RefAttributes<HTMLDivElement>>;
export default CDSContainer;
