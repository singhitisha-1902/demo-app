import { type ComponentPropsWithRef } from 'react';
import { type Margin } from '@ciscodesignsystems/cds-component-types';
import type { CDSSpinnerLabelPlacement, CDSSpinnerSize } from './';
export interface CDSSpinnerProps extends ComponentPropsWithRef<'div'> {
    /**
     * Provides size to the `Spinner` component.
     *
     * Accepts t-shirt sizes (`sm`, `md`, `lg`, `xl`)
     * @default md
     */
    size?: CDSSpinnerSize;
    /**
     * Provides access to add margin to spinner.
     *
     * Accepts type MarginNumber
     | [MarginNumber]
     | [MarginNumber, MarginNumber]
     | [MarginNumber, MarginNumber, MarginNumber]
     | [MarginNumber, MarginNumber, MarginNumber, MarginNumber]
     | MarginObject
     *
     * @default 0
     */
    margin?: Margin;
    /**
     * An optional label for the spinner.
     *
     * @default ''
     */
    label?: string;
    /**
     * Placement of the spinner label.
     *
     * @default 'bottom'
     */
    labelPlacement?: CDSSpinnerLabelPlacement;
}
export declare const CDSSpinner: import("react").ForwardRefExoticComponent<Omit<CDSSpinnerProps, "ref"> & import("react").RefAttributes<HTMLDivElement>>;
export default CDSSpinner;
