'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var jsxRuntime = require('react/jsx-runtime');
var react = require('react');
var cdsComponentUtilities = require('@ciscodesignsystems/cds-component-utilities');
var skeleton_module = require('./skeleton.module.cjs.js');

const CDSSkeleton = ({
  className = '',
  height = 'md',
  lines = 1,
  oblique = false,
  shade = 'regular',
  style,
  ...rest
}) => {
  const computedStyle = typeof height === 'number' ? {
    ...style,
    height
  } : style;
  const computedClassName = cdsComponentUtilities.cx(skeleton_module["default"], {
    'cds-skeleton': true,
    [`cds-skeleton--${height}`]: typeof height !== 'number',
    [`cds-skeleton--${shade}`]: true
  }, className);
  return jsxRuntime.jsx(jsxRuntime.Fragment, {
    children: Array(lines).fill(undefined).map((_, index) => {
      const uniqueKey = `item_${index}`;
      return /*#__PURE__*/react.createElement("div", {
        ...rest,
        className: computedClassName,
        key: uniqueKey,
        style: {
          ...computedStyle,
          ...(oblique ? {
            width: `${100 - 5 * index}%`
          } : {})
        }
      });
    })
  });
};

exports.CDSSkeleton = CDSSkeleton;
