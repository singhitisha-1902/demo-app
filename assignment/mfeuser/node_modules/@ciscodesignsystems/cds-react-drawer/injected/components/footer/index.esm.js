import { cx } from '@ciscodesignsystems/cds-component-utilities';
import { CDSFlex } from '@ciscodesignsystems/cds-react-flex/injected';
import styles from '../../drawer.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSDrawerFooter = ({
  children,
  className = '',
  ...rest
}) => /*#__PURE__*/jsx(CDSFlex, {
  align: "center",
  className: cx(styles, `cds-drawer__footer ${className}`),
  gap: "sm",
  justify: "flex-end",
  role: "toolbar",
  ...rest,
  children: children
});
CDSDrawerFooter.displayName = 'CDSDrawer.Footer';

export { CDSDrawerFooter };
