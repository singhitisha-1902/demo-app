import { type CDSSelectSize } from '../../types';
export type CDSSelectAddItemProps = {
    inputValue: string;
    handleAddClick: () => void;
    size?: CDSSelectSize;
};
export declare const CDSSelectAddItem: ({ inputValue, handleAddClick, size }: CDSSelectAddItemProps) => import("react/jsx-runtime").JSX.Element;
