import { type ReactNode } from 'react';
export declare const MIN_TRUNCATION_DEFAULT = 5;
export declare const truncateText: (children: ReactNode, truncated: boolean | number | undefined) => ReactNode;
