import type { CDSOption, CDSOptionGroup } from './types';
type useTypeaheadProps = {
    inputValue: string;
    items: CDSOption[] | CDSOptionGroup[] | any[];
    enabled?: boolean;
};
export declare function useTypeahead(props: useTypeaheadProps): any[];
export {};
