import { type Dispatch, type SetStateAction } from 'react';
type useControllableStateProps<T> = {
    value?: T;
    defaultValue?: T;
    onChange?: (value: T) => void;
};
export declare function useControllableState<T>({ value: valueFromProps, onChange, defaultValue, }: useControllableStateProps<T>): [T, Dispatch<SetStateAction<T>>];
export {};
