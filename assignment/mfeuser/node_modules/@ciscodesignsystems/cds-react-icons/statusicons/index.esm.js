import { forwardRef } from 'react';
import { cx } from '@ciscodesignsystems/cds-component-utilities';
import { handleStatus } from '../util/status-helper.esm.js';
import styles from './status-icon.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSStatusIcon = /*#__PURE__*/forwardRef((props, ref) => {
  const {
    status,
    variation,
    className,
    ...rest
  } = props;
  const Svg = handleStatus(status);
  const classes = cx(styles, 'cds-status-icon', {
    'cds-status-icon--border': variation === 'bordered'
  }, className);
  return /*#__PURE__*/jsx(Svg, {
    className: classes,
    ref: ref,
    ...rest
  });
});
CDSStatusIcon.displayName = 'CDSStatusIcon';
CDSStatusIcon.defaultProps = {
  variation: 'none'
};

export { CDSStatusIcon, CDSStatusIcon as default };
