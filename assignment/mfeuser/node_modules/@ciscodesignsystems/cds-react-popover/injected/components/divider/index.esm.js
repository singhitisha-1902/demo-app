import { forwardRef } from 'react';
import { cx } from '@ciscodesignsystems/cds-component-utilities';
import styles from './divider.module.esm.js';
import { jsx } from 'react/jsx-runtime';

const CDSPopoverDivider = /*#__PURE__*/forwardRef((props, ref) => {
  const {
    className,
    ...rest
  } = props;
  const classNames = cx(styles, 'cds-popover__divider', className);
  return /*#__PURE__*/jsx("hr", {
    className: classNames,
    ref: ref,
    ...rest
  });
});
CDSPopoverDivider.displayName = 'CDSPopover.Divider';

export { CDSPopoverDivider };
