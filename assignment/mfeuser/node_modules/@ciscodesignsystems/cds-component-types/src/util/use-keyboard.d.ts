export type KeyboardEventType = 'keydown' | 'keyup' | 'keypress';
declare type UseKeyboardOptions = {
    /**
     * A set of keys that will restrict which keys need to be pressed in order to fire the event.
     *
     * Defaults to `[]`.
     */
    keys?: string[];
    /**
     * The type of keyboard event you want to listen to.
     *
     * Defaults to `keyup`.
     */
    type?: KeyboardEventType;
};
/**
 * A hook that will introduce a customizable keyboard event listener which fires a specific callback.
 *
 * @param callback The event that will get fired when the keyboard event is fired.
 * @param options A set of options that allows for customization of the behaviour that's attached to the event listener.
 */
export declare const useKeyboard: (callback: () => void, options?: UseKeyboardOptions) => void;
export {};
